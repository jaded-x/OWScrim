const { ActionRowBuilder, ModalBuilder, TextInputBuilder, TextInputStyle, Client } = require('discord.js')

module.exports = {
    name: 'scrim',
    description: 'Scrims will be shown in the channel this command is executed in.',
    async execute(interaction) {
        
    }
}